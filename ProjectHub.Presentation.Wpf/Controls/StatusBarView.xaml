<UserControl
    x:Class="ProjectHub.Presentation.Wpf.Controls.StatusBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:ProjectHub.Application.ViewModels;assembly=ProjectHub.Application"
    d:DataContext="{d:DesignInstance viewmodels:SidebarViewModel}"
    d:DesignHeight="32"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Border
        Background="{DynamicResource BgTertiaryBrush}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="0,1,0,0">
        <Grid Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Project Count  -->
            <TextBlock
                Grid.Column="0"
                Margin="24,0,0,0"
                VerticalAlignment="Center"
                Style="{StaticResource StatusTextStyle}"
                Text="{Binding ProjectCountText}" />

            <!--  Folder Count  -->
            <TextBlock
                Grid.Column="1"
                Margin="24,0,0,0"
                VerticalAlignment="Center"
                Style="{StaticResource StatusTextStyle}"
                Text="{Binding FolderCountText}" />

            <!--  Workspace Count  -->
            <TextBlock
                Grid.Column="2"
                Margin="24,0,0,0"
                VerticalAlignment="Center"
                Style="{StaticResource StatusTextStyle}"
                Text="{Binding WorkspaceCountText}" />

            <!--  Tag Count  -->
            <TextBlock
                Grid.Column="3"
                Margin="24,0,0,0"
                VerticalAlignment="Center"
                Style="{StaticResource StatusTextStyle}"
                Text="{Binding FavouriteText}" />

        </Grid>
    </Border>
</UserControl>
